# agents/

存放 **Agent（智能体）** 定义，文件扩展名 `.agent.md`。

每个文件 = YAML front-matter（含 `id:` 稳定身份、name/description/category/tools/skills/model 等）+ Markdown 正文（系统提示词）。

在 labs 页点击文件即打开「上表单 + 下源码」两栏编辑；右键「新建 → Agent」创建。